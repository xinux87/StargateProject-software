import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test placeholder', (WidgetTester tester) async {
    // Actual app tests require BLE — placeholder to keep analyzer happy.
    expect(true, isTrue);
  });
}
